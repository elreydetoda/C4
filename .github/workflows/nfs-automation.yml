name: NFS Automation

on:
  issues:
    types: [opened]

jobs:
  handle_new_issue:
    runs-on: ubuntu-latest
    steps:
      - name: Handle new issue
        id: new-issue
        uses: ./actions/nfs-automation
        with:
          repo-token: ${{ secrets.GH_TOKEN }}